function Maze(map) {

    this.map = map || null;
}

Maze.prototype.shortestPath = function(source, destination) {
};


Maze.prototype.getPaths = function(source, destination) {
    var paths = [];

    var possibleCells = [];

    return paths;
};

module.exports = Maze;

